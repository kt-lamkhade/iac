name: First workflow
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run commands
        run: |
          echo "Hello, world!"
          echo "This is my first workflow."