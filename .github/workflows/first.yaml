name: First workflow
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run commands
        run: |
          echo "Hello, world!"
          echo "This is my first workflow."
          
      - name: List files
        run: ls -la