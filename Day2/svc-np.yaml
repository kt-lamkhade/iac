apiVersion: v1
kind: Service
metadata:
  name: np-svc
  namespace: kiran
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
  - port: 80  #service port inside cluster
    targetPort: 80 #container port
    nodePort: 30050