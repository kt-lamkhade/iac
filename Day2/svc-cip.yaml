apiVersion: v1
kind: Service
metadata:
  name: cip-svc
  namespace: kiran
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
  - port: 80  #service port inside cluster
    targetPort: 80 #container port